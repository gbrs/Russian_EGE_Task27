'''
положительные целые числа, не превышающее 1000.
После передаётся контрольное значение — наибольшее число R:
1) R — произведение двух чисел, переданных в разные минуты;
2) R делится на 26.
Напечатать отчёт по следующей форме:
Вычисленное контрольное значение: …
Контроль пройден (или Контроль не пройден)
'''

'''
Ответом является наибольшее из двух произведений:
1) максимальное нечетное, кратное 13, умноженное на максимальное четное число из оставшихся;
2) максимальное число, кратное 26, умноженное на максимальное число из оставшихся.
Чтобы не взять в произведении одно и тоже число, можно хранить номер их поступления.
Если вдруг найдем повторяющиеся значения по этому номеру, то 
найдем предмаксимумы для обоих (?) случаев и возьмем более выгодный из них.
'''

n = int(input())  # количество вводимых значений
Max = max26 = max13 = max2 = 0  # максимум и максимальные кратные 26, 13 и 2
ans = ans1 = ans2 = 0
R = 0

# принимаем все числа, одновременно находя максимальное, второе по размеру, максимальные кратные 2, 13 и 26
# в этой программе, похоже, наплевали на чистоту максимумов: max13, max2 и max26 могут совпадать
# впрочем это, похоже, удлиняет программу на одну строчку: if index2 != index13
for i in range(n):
    a = int(input())
    if a % 13 == 0 and a > max13:  # макс.число кратное 13 и его порядковый номер
        max13 = a
        index13 = i
    if a % 2 == 0 and a > max2:  # макс.четное число и его порядковый номер
        max2 = a
        index2 = i
    if a % 26 == 0 and a > max26:  # макс.число кратное 26 и его порядковый номер
        max26 = a
        index26 = i
    # макс.число и его порядковый номер.
    # Зачем в условии и случай равенства? Он справляется с ситуацией одинаковых максимальных значений
    if a >= Max:
        pre_Max = Max  # в pre_max запоминаем второе по величине число
        Max = a
        index_Max = i
    # сравниваем текущее вводимое значение со вторым по величине числом
    if a < Max and a >= pre_Max:  # a = Max рассмотрена в предыдущем if. Зачем менять значение при a = pre_Max?
        pre_Max = a

R = int(input())  # принимаем контрольное значение

# вычисление максимальных значений max2 * max13 и max26 * X
if index2 != index13:  # соблюдается, если самым большим кратным 13 или 2 не было число кратное 26
    ans1 = max2 * max13
if index26 != index_Max:  # максимумы, связанные с max26
    ans2 = Max * max26
else:
    ans2 = pre_Max * max26  # если максимум  и max26 - одно число, то умножаем на второе по величине число

# выбираем максимальное из двух вариантов: max2 * max13 и max26 * X
if ans1 > ans2:
    ans = ans1
else:
    ans = ans2
print('Вычисленное контрольное значение:', ans)

# сравниваем свое контрольное значение с принятым и сообщаем результат
if ans == R:
    print('Контроль пройден')
else:
    print('Контроль не пройден')
